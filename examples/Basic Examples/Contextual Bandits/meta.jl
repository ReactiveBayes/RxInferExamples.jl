return (
    title = "Contextual Bandits",
    description = """
    This notebooks covers RxInfer usage for the Contextual Bandits problem.
    """,
    tags = ["basic examples", "contextual bandits"],
    # Among the slowest examples to build - start it first so it does not become the
    # tail that determines the total build time. See `docs/src/how_build_works.md`.
    build_priority = true
)
